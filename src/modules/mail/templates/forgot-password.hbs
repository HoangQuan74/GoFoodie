<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }

        p {
            color: #727272;
            font-size: 16px;
        }

        .header {
            position: relative;
            background-color: #107953;
            border-radius: 16px 16px 0 0;
            height: 100px;
            overflow: hidden;
            text-align: center;
        }

        .header-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url({{headerImage}});
            background-size: cover;
            opacity: 0.2;
            z-index: 0;
        }

        .header span {
            position: relative;
            z-index: 999; 
            font-weight: 900;
            font-size: 24px;
            color: white;
            line-height: 100px;
        }
        
        .logo-container {
            text-align: center;
            padding: 30px 0;
        }

        .logo {
            max-width: 200px;
        }

        .otp-container {
            background-color: #107953;
            color: white;
            text-align: center;
            font-size: 32px;
            font-weight: 900;
            letter-spacing: 5px;
            margin: 20px 0;
            border-radius: 4px;
            height: 72px;
            line-height: 72px;
        }

        .warning {
            color: #727272;
            margin: 20px 0;
        }

        .warning span {
            color: #121212;
            font-weight: 700;
        }
        
        .signature {
            margin-top: 40px;
            font-weight: 700;
            color: #727272;
        }

    </style>
</head>
<body>
    <div class="header">
        <div class="header-bg"></div>
        <span>Chào mừng bạn đến với ONESHIP!</span>
    </div>
    
    <div class="logo-container">
        <img src="{{logo}}" alt="ONESHIP" class="logo">
    </div>

    <h2>Xin chào {{name}}</h2>
    
    <p>ONESHIP vừa nhận được yêu cầu xác minh tài khoản của bạn. Bạn vui lòng nhập mã OTP bên dưới để xác minh tài khoản:</p>
    
    <div class="otp-container">
        {{otp}}
    </div>
    
    <p class="warning">
        <span>Lưu ý:</span> KHÔNG chia sẻ mã này với bất kỳ ai, kể cả nhân viên ONESHIP!
    </p>
    
    <div class="signature">
        <p>Xin cảm ơn!</p>
        <p>ONESHIP Team</p>
    </div>
</body>
</html>