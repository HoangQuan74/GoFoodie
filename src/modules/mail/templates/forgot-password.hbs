<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }

        .header:before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url({{headerImage}});
            background-size: cover;
            opacity: 0.2;
        }
        
        .header {
            position: relative;
            background-color: #107953;
            color: white;
            padding: 15px;
            text-align: center;
            border-radius: 8px 8px 0 0;
            font-weight: 900;
            z-index: 1;
        }
        .logo-container {
            text-align: center;
            padding: 30px 0;
        }
        .logo {
            max-width: 200px;
        }
        .otp-container {
            background-color: #107953;
            color: white;
            padding: 20px;
            text-align: center;
            font-size: 24px;
            letter-spacing: 5px;
            margin: 20px 0;
            border-radius: 4px;
        }
        .warning {
            color: #666;
            margin: 20px 0;
        }
        .signature {
            margin-top: 40px;
            color: #666;
        }
        .tagline {
            color: #0D8A6A;
            font-size: 14px;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="header">
        Chào mừng bạn đến với ONESHIP!
    </div>
    
    <div class="logo-container">
        <img src="{{logo}}" alt="ONESHIP" class="logo">
        <div class="tagline">Mọi món ngon, gắn trong tích tắc</div>
    </div>

    <h2>Xin chào {{name}}</h2>
    
    <p>ONESHIP vừa nhận được yêu cầu xác minh tài khoản của bạn. Bạn vui lòng nhập mã OTP bên dưới để xác minh tài khoản:</p>
    
    <div class="otp-container">
        {{otp}}
    </div>
    
    <p class="warning">
        Lưu ý: KHÔNG chia sẻ mã này với bất kỳ ai, kể cả nhân viên ONESHIP!
    </p>
    
    <div class="signature">
        <p>Xin cảm ơn!</p>
        <p>ONESHIP Team</p>
    </div>
</body>
</html>