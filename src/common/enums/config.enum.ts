export enum EConfigType {
  AppDriver = 'app_driver',
  AppMerchant = 'app_merchant',
  WebAdmin = 'web_admin',
  AppClient = 'app_client',
}

export enum EAppType {
  AppDriver = 'app_driver',
  AppMerchant = 'app_merchant',
  AppClient = 'app_client',
}
