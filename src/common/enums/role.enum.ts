export enum Role {
  User = 'user',
  Admin = 'admin',
}

export enum ERoleType {
  Client = 'client',
  Merchant = 'merchant',
  Driver = 'driver',
  Admin = 'admin',
}

export enum EUserType {
  Client = 'client',
  Merchant = 'merchant',
  Driver = 'driver',
}
