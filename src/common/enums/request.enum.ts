export enum ERequestStatus {
  Pending = 'pending',
  Approved = 'approved',
  Rejected = 'rejected',
}

export enum ERequestType {
  Create = 'create',
  Update = 'update',
}

export enum EDriverRequestType {
  Other = 'other',
}
