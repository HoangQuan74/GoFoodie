export enum ETransactionStatus {
  Pending = 'pending',
  Success = 'success',
  Failed = 'failed',
}

export enum ETransactionType {
  Deposit = 'deposit',
  Withdraw = 'withdraw',
  Transfer = 'transfer',
}
