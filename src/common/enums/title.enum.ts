export enum ETitlePolicyType {
  Add = 'add',
  Subtract = 'subtract',
}

export enum ETitlePolicyFrequency {
  Daily = 'daily',
  Weekly = 'weekly',
  Monthly = 'monthly',
}

export enum ETitleIconPosition {
  Left = 'left',
  Right = 'right',
  Inside = 'inside',
}
